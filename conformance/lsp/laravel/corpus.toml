# The Gate imageboard — a real Laravel 13 app used as Laravel LSP's workspace
# for framework probes (env, config, route, view, translation). Positions are
# text-anchored in corpus-probes.toml and resolved against this checkout.
# The commit is pinned so a `make pull-submodules` that moves HEAD fails
# loudly until the probes are re-anchored.

root = "references/laravel-gate-image-board"
commit = "5cd1e3064ebdb432e543b4f4f313036bd0afcd8a"
project = "rigortype/laravel-gate-image-board"
