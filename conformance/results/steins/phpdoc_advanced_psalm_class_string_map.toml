status = "Falls back to mixed"
conformance_automated = "Pass"
expected_diagnostic_count = 1
output = "phpdoc_advanced_psalm_class_string_map.php:19: `@param class-string-map<T of \\Throwable, T> $map` does not parse (expected CloseAngle, found Identifier) — the tag declares nothing [phpdoc.unparsable]"
errors_diff = ""
notes = "Psalm's `T of X` template bound isn't valid inside a plain generic-argument list in steins-phpdoc's grammar, so `class-string-map<T of \\Throwable, T>` fails to parse entirely and the whole `@param` drops to no envelope (ADR-0029) — the dependent key-to-value relationship is never checked."
ignore_errors = []
recognition = "unrecognized"
enforcement = "none"
enforced_lines = "0/1"
unrecognized_lines = [19]
false_positive_lines = []
