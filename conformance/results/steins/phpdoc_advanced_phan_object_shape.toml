status = "Falls back to mixed"
conformance_automated = "Pass"
expected_diagnostic_count = 1
output = "phpdoc_advanced_phan_object_shape.php:32: argument \"Ada\" (from $obj->name) to takesInt() cannot become int $value — proven TypeError (strict mode) [type.argument-mismatch]"
errors_diff = ""
notes = "steins-phpdoc's object-shape grammar only fires on the literal `object` keyword before `{` (parser.rs), not an arbitrary class name, so `stdClass{name: string}` fails to parse as a shape and the whole annotation drops to no envelope (ADR-0029) — the per-property constraint is never checked."
ignore_errors = []
recognition = "recognized"
enforcement = "enforced"
enforced_lines = "1/1"
unrecognized_lines = []
false_positive_lines = []
over_rejected_lines = []
