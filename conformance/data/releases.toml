# Latest upstream release of every artifact the report describes, as shown in
# the "Latest release" column of both reference tables.
#
# This is the injected half of the metadata: everything else about a tool --
# who founded it, what entity is behind it, what it parses with -- is a curated
# judgement hardcoded in src/Metadata/Analyzer/<Tool>.php or
# src/Metadata/LanguageServer/<Tool>.php, and only changes when the project
# itself does. These two fields go stale on their own, so they live here where
# a script can rewrite the file wholesale from upstream release feeds without
# touching any curation.
#
# Intelephense, Psalm and PHPantom have one entry apiece, read by both
# tables: each is the same artifact reached two ways, so a single release
# date is the honest answer. devsense-php-ls and phpy are the opposite case
# -- one engine, two separately versioned artifacts -- and so get an entry
# each.
#
# Not to be confused with results/<tool>/version.toml, which records the
# version this suite actually ran. This file records what upstream has shipped
# most recently, which may be newer.
#
# Keys are the runner's tool names. `version` is whatever upstream calls the
# release -- "unversioned" where a project ships without version numbers --
# and `date` is an ISO 8601 calendar date.

# Analyzers.

[phan]
version = "6.0.7"
date = "2026-06-22"

[psalm]
version = "6.16.1"
date = "2026-03-19"

[phpstan]
version = "2.2.8"
date = "2026-08-04"

[intelephense]
version = "1.18.5"
date = "2026-06-21"

[noverify]
version = "0.5.5"
date = "2025-04-22"

[qodana]
version = "262.9437.196"
date = "2026-08-10"

[mago]
version = "1.46.0"
date = "2026-08-05"

[phpy]
version = "1.0.18519"
date = "2026-02-19"

[phpantom]
version = "0.9.0"
date = "2026-07-19"

[mir]
version = "0.70.1"
date = "2026-08-06"

[pzoom]
version = "unversioned"
date = "2026-06-24"

[steins]
version = "0.1.5"
date = "2026-08-12"

# Language servers.

[phpactor]
version = "2026.07.22.0"
date = "2026-07-22"

[devsense-php-ls]
version = "1.0.19148"
date = "2026-08-09"

[php-lsp]
version = "0.24.1"
date = "2026-08-11"

# Reference-only: function-specific servers are listed, not measured.

[phpunit-language-server]
version = "0.3.7"
date = "2018-04-25"
